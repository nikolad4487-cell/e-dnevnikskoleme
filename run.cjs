const fs = require('fs');

const file = 'src/pages/admin/StudentSubjectEnrollmentPage.tsx';

// I will just construct the AST-like replacement using replace and regex, or replace the whole file. 
// A script to overwrite the logic.
